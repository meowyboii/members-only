<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link href="/styles.css" rel="stylesheet" />
  </head>
  <body>
    <main
      class="flex justify-center items-center min-h-screen bg-gradient-to-r from-teal-900 to-emerald-500"
    >
      <div class="flex flex-col justify-center bg-zinc-700 w-xl p-10">
        <h1 class="text-center">Sign In</h1>
        <form
          action="/log-in"
          method="post"
          class="flex flex-col space-y-3 p-5 w-full"
        >
          <label for="username">Username</label>
          <input id="username" name="username" type="text" />
          <label for="password">Password</label>
          <input
            id="password"
            name="password"
            type="password"
            autocomplete="new-password"
          />

          <button class="btn-primary">Sign In</button>
          <div class="text-zinc-400">
            Don't have an account?
            <a href="/sign-up" class="text-teal-500 hover:underline"
              >Register</a
            >
          </div>

          <% if(error && error.length > 0){%>
          <section class="md:w-2xl">
            <p class="text-red-500"><%= error[0] %></p>
          </section>
          <%} %>
        </form>
      </div>
    </main>
  </body>
</html>
